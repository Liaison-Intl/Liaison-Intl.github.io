$(function () {
  // We're generating some of the Markdown we use, so apply styles dynamically.
  $('h2:first').addClass('page-header');
  $('p:first').addClass('lead');
  $('table').addClass('table table-striped');
});
